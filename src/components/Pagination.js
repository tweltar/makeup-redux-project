import React from 'react';
import './Pagination.css';

const Pagination = () => {
    return (
        <nav className="Pagination">
            <ul>
                {
                    
                }
            </ul>
        </nav>
    );
};

export default Pagination;